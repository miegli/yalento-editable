import { YalentoEditableDirective } from './yalento-editable.directive';

describe('YalentoEditableDirective', () => {
  it('should create an instance', () => {
    const directive = new YalentoEditableDirective();
    expect(directive).toBeTruthy();
  });
});
